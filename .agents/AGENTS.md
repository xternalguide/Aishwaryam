# Workspace Rules

- **Reuse Existing Code:** Always prioritize using and extending the existing system architecture, files, and utility methods. Do not rebuild components, helpers, or setups from scratch if they already exist in the codebase.
